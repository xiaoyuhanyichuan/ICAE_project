# 面向既有风冷数据中心低碳改造的协同规划模型

## 1. 建模定位

本文研究对象为既有风冷数据中心在向智算业务承载场景转型时的低碳改造规划问题。与原研究问题相比，新模型不再以单个机柜或机柜内部设备组作为独立改造对象，而是以“空调机组负责的机柜组”为统一改造单元。优化模型需要在液冷渗透率、余热回收能力、空气源/水源热泵容量、电/热储能容量以及既有风冷基础设施调整之间进行协同规划，在满足热安全、冷却链容量、供热需求和储能运行约束的基础上，得到年化总成本与年化碳排放之间的 Pareto 最优权衡。

原模型中已经具备以下可继承模块：

- 机柜级 IT 负荷输入与热点温升代理模型。
- 风冷和液冷承担热负荷的分解框架。
- 余热回收、热泵、蓄热、电池储能和供热平衡约束。
- 年化总成本与年化总碳排放的双目标评价。
- 外层 NSGA-II 规划搜索与内层 MILP 运行调度评估框架。

新模型的核心修改为：

- 将原规划变量 $y_i$、$x_{ig}$ 替换为机组级构型选择变量 $u_{z,k}$。
- 引入空调机组老化率，使既有风冷容量和效率随机组差异下降。
- 引入业务负荷爬升系数，使复制后的机房样本具有不同智算负荷增长强度。
- 将空气源热泵和水源热泵分开建模，以对应热通道空气余热和水侧余热两类可回收热源，其中水侧余热包括冷板/CDU 侧热量和 RDHX 背板水侧热量。
- 显式引入 Chiller 冷源约束：只有空调末端和 RDHX 背板需要由冷机提供冷源；冷板改造通过 CDU 后可进入水源热泵或自然冷却排热侧，但不进入 Chiller 排热链。
- 显式引入 CDU 容量规划：选择冷板构型的机柜组需要配置 CDU，CDU 容量限制冷板回路可承接的液冷热负荷。
- 取消液冷侧直接供热口径：冷板/CDU 侧热量和 RDHX 背板水侧余热均需先经水源热泵升级后才能用于供热；未被水源热泵利用的冷板/CDU 热量进入自然冷却排热侧。
- 根据资料调研，将 RDHX 背板式冷却建模为可吸收其负责机柜组 100% IT 热量的近端液冷末端，因此 RDHX 链路不再设置辅助风冷/房间冷却容量。
- 将 BESS 由“能量容量 + 功率容量”两维规划改为整体容量规划，成本和隐含碳仅按整体 BESS 容量计入，运行功率上限通过固定倍率参数由整体容量派生。
- 将实验方案固定为 baseline1、baseline2 与精细化改造方案三类对比。

## 2. 数据与场景构造

已有数据包括：

- `time_series.csv`：49 个机柜的逐时 IT 功率曲线，列名为 `rack_it_000_kw` 至 `rack_it_048_kw`，并包含总 IT 功率列 `it_total_kw`。
- `rack_metadata.csv`：49 个机柜的空间位置、服务器类型混合比例、热点风险和容量代理信息。
- `H_matrix.csv`：49 个机柜之间的热耦合影响矩阵，维度为 $49\times49$。
- 空调机组与机柜的对应关系由 RF 拟合结果给出。记原始机房中第 $z_0$ 个空调机组负责的机柜集合为 $R_{z_0}^{0}$。

为扩展实验规模，将原始机房复制 5 份：

$$
C=\{1,2,3,4,5\}
$$

复制后的机柜集合为：

$$
R=\{(c,i_0)\mid c\in C,\ i_0\in R^0\}
$$

复制后的空调机组集合为：

$$
Z=\{(c,z_0)\mid c\in C,\ z_0\in Z^0\}
$$

其中 $|R^0|=49$，$|Z^0|=6$，因此 $|R|=245$，$|Z|=30$。若 $z=(c,z_0)$，则其负责的机柜组为：

$$
R_z=\{(c,i_0)\mid i_0\in R_{z_0}^{0}\}
$$

机柜间热耦合矩阵在每个复制机房内沿用原始 $H^0$，不同复制机房之间默认没有热耦合：

$$
H_{(c,j_0),(c',i_0)}=
\begin{cases}
H^0_{j_0 i_0}, & c=c'\\
0, & c\ne c'
\end{cases}
$$

本文所有上标 $0$ 均表示“原始/基准”含义，不表示幂次。例如 $R^0$、$Z^0$、$H^0$ 分别表示复制前的原始机柜集合、原始空调机组集合和原始热耦合矩阵；$\widehat q_{i,t}^{0}$ 表示未叠加业务爬升前的基准 IT 负荷曲线。复制后的集合和变量不再带上标 $0$。

## 3. 集合与索引

| 符号 | 含义 |
|---|---|
| $i,j\in R$ | 复制后的机柜索引 |
| $z\in Z$ | 复制后的空调机组索引 |
| $R_z$ | 空调机组 $z$ 负责的机柜集合 |
| $k\in K$ | 改造构型索引，$K=\{0,1,\dots,6\}$ |
| $t\in T$ | 运行时段索引 |
| $\omega\in\Omega$ | 典型日或典型场景索引，可在实现中替代全年逐时集合 |

若使用典型日降维，可将所有运行期求和写为：

$$
\sum_{t\in T}(\cdot)
$$

或在实现中替换为：

$$
\sum_{\omega\in\Omega} w_\omega \sum_{h\in H}(\cdot)
$$

其中 $w_\omega$ 为典型日代表的全年天数权重。

## 4. 七类改造构型

对每个空调机组 $z$，其负责的机柜组 $R_z$ 必须统一选择一种改造构型。

| $k$ | 构型名称 | 风冷基础设施 | 热通道空气余热 | 液冷/背板热量路径 |
|---|---|---|---|---|
| 0 | 保留老旧空调 | 保留老旧空调 | 不回收 | 无 |
| 1 | 替换新空调 | 替换为新空调 | 不回收 | 无 |
| 2 | 老旧空调 + 空气源热泵 | 保留老旧空调 | 接空气源热泵 | 无 |
| 3 | 新空调 + 空气源热泵 | 替换为新空调 | 接空气源热泵 | 无 |
| 4 | 老旧空调 + 冷板 + 双热泵/自然冷却 | 保留老旧空调 | 接空气源热泵 | 冷板经 CDU 接水源热泵或自然冷却排热 |
| 5 | 背板式冷却 + 水源热泵 | 老旧空调改为背板式冷却 | 无剩余空气侧热量 | 背板水侧接水源热泵 |
| 6 | 新空调 + 冷板 + 双热泵/自然冷却 | 替换为新空调 | 接空气源热泵 | 冷板经 CDU 接水源热泵或自然冷却排热 |

构型 5 的建模口径建议为：背板式冷却作为近端液冷末端承担该机柜组 100% IT 热负荷，不再额外保留辅助风冷或房间冷却能力，也不再把老旧空调作为该机柜组的主体制冷设备。RDHX 背板的冷源由 Chiller 提供，背板回水中的可利用余热只能进入水源热泵升级后供热；未进入水源热泵的 RDHX 热量仍由 Chiller 承担。

构型 4 和构型 6 的冷板回路需要通过 CDU 与设施侧水系统换热。CDU 不是冷机负荷，但 CDU 后的冷板热量可以作为水源热泵热源；未被水源热泵利用的部分再进入自然冷却侧直接向环境排热。CDU 决定冷板侧可接入的液冷容量，并带来投资、隐含碳、重量和泵耗。因此冷板构型下应同时规划 $Cap_z^{CDU}$。

## 5. 关键参数

本节只列外生参数、由原始数据预处理得到的参数，以及可由工程手册或情景假设给定的参数；规划层和运行层决策变量分别见第 6 节和第 7 节。

### 5.1 IT 负荷与业务爬升

| 符号 | 含义 |
|---|---|
| $\widehat q_{i,t}^{0}$ | 机柜 $i$ 对应原始 M100 曲线的基准 IT 负荷 |
| $\rho_i^{load}$ | 机柜 $i$ 的业务负荷爬升强度 |
| $\tau_t$ | 归一化规划时间或业务爬升进度，$\tau_t\in[0,1]$ |
| $Y_t$ | 相对基准年的规划年数，用于指数型负荷爬升 |
| $Q_i^{IT,max}$ | 机柜 $i$ 可承载的 IT 负荷上限 |

业务负荷爬升可写为：

$$
\widehat q_{i,t}^{IT}
=
\min\left\{
Q_i^{IT,max},
\widehat q_{i,t}^{0}\left(1+\rho_i^{load}\tau_t\right)
\right\}
$$

若论文希望表达多年连续增长，也可将线性爬升替换为指数爬升：

$$
\widehat q_{i,t}^{IT}
=
\min\left\{
Q_i^{IT,max},
\widehat q_{i,t}^{0}\left(1+\rho_i^{load}\right)^{Y_t}
\right\}
$$

其中 $Y_t$ 表示相对基准年的年数。复制 5 份机房时，可为不同复制机房设置不同的 $\rho_i^{load}$ 分布，以模拟智算业务爬升速度的差异。

当前代码实验阶段缺少真实业务爬升数据，因此将 $\rho_i^{load}$ 作为机柜级可复现实验输入模拟：

$$
\rho_i^{load}\sim U(0.05,0.25),\quad seed=20260601
$$

模拟值进入输入负荷曲线，而不是仅作为事后解释变量；每次优化运行均需记录机柜级 $\rho_i^{load}$。

### 5.2 空调老化

| 符号 | 含义 |
|---|---|
| $Cap_z^{old}$ | 老旧空调机组 $z$ 的额定制冷容量 |
| $\alpha_z^{age}$ | 空调机组 $z$ 的容量老化率，$0\le\alpha_z^{age}<1$ |
| $\chi_z^{cap}=1-\alpha_z^{age}$ | 空调机组 $z$ 的剩余容量系数 |
| $COP_z^{old,0}$ | 未老化时老旧空调的名义 COP |
| $\epsilon_z^{age}$ | 空调机组 $z$ 的能效衰减率 |
| $\psi_t^{amb}$ | 环境工况对风冷容量的修正系数，反映室外温湿度或冷源供回水条件变化 |

老旧空调的有效容量为：

$$
Cap_{z,t}^{old,eff}
=
\chi_z^{cap}\psi_t^{amb}Cap_z^{old}
$$

当前代码实验阶段不启用环境工况降额，固定取 $\psi_t^{amb}=1$。因此实际输入优化模型的老旧空调有效容量为：

$$
Cap_{z}^{old,eff}
=
(1-\alpha_z^{age})Cap_z^{old}
$$

老旧空调的有效 COP 为：

$$
COP_z^{old,eff}
=
(1-\epsilon_z^{age})COP_z^{old,0}
$$

不同复制机房可设置不同的 $\alpha_z^{age}$ 和 $\epsilon_z^{age}$，从而体现既有空调机组服役年限和健康状态差异。若显式建模 Chiller，则 $COP_z^{old,eff}$ 不再用于计算冷机压缩功，而可仅作为与旧模型衔接的简化参数；主模型中空调末端耗电由 $P_t^{terminal}$ 表示，冷机耗电由 $P_t^{Chiller}$ 表示。

$\psi_t^{amb}$ 不宜凭空优化得到，而应作为外生参数给定。若有设备样本或厂家性能曲线，可由室外干球温度、湿球温度或冷冻水供水温度拟合：
$$
\psi_t^{amb}=f(T_t^{out},T_t^{wb},T_t^{chw,s})
$$

并将其归一化到 $[0,1]$。但在当前实验版本中，$\psi_t^{amb}$ 不进入优化敏感性，统一取 $1$，把空调退化主要放入 $\alpha_z^{age}$ 和 $\epsilon_z^{age}$。

由于缺少真实空调组老化数据，当前代码按空调机组/机柜组尺度模拟：

$$
\alpha_z^{age}\sim U(0.08,0.25),\quad seed=20260601
$$

若没有单独效率退化率，取 $\epsilon_z^{age}=\alpha_z^{age}$，并用于老旧空调末端单位冷量耗电系数修正。模拟值需与机柜所在区域关联记录，供结果图分析构型选择与业务爬升、空调老化之间的关系。

### 5.3 改造构型与液冷分流参数

| 符号 | 含义 |
|---|---|
| $C_{z,k}^{retro}$ | 空调机组 $z$ 选择构型 $k$ 触发的一次性固定改造成本，仅包括拆改、接口、管路接驳、施工安装和停机损失等不随容量连续变化的成本，不包括新空调、RDHX、CDU、热泵、冷机、冷却塔和储能等容量设备投资 |
| $EI_{z,k}^{retro}$ | 空调机组 $z$ 选择构型 $k$ 触发的固定改造隐含碳，仅包括构型改造本身的隐含碳，不包括容量设备隐含碳 |
| $n_k$ | 构型 $k$ 对应改造设备的折算寿命 |
| $\eta_i^{CP}$ | 机柜 $i$ 接入冷板后由液体带走的热量比例 |
| $\eta_i^{RD}$ | RDHX 背板水侧带走 IT 热量比例；本文按资料调研取 $\eta_i^{RD}=1$，即 RDHX 可吸收其负责机柜 100% IT 热量 |

### 5.4 容量上限与既有冷源参数

| 符号 | 含义 |
|---|---|
| $\overline{Cap}_z^{AC,new}$ | 新空调可安装容量上限 |
| $\overline{Cap}_z^{RDHX}$ | 背板式冷却末端可安装水侧容量上限 |
| $\overline{Cap}_z^{CDU}$ | 冷板构型下 CDU 可安装容量上限 |
| $Cap^{Chiller,old}$ | 既有 Chiller 可用制冷容量 |
| $Cap^{Tower,old}$ | 既有冷却塔可用排热容量 |
| $\gamma^{Chiller}$ | Chiller 容量安全裕度系数 |
| $\gamma^{Tower}$ | 冷却塔容量安全裕度系数 |

### 5.5 热安全、热耦合与环境工况参数

| 符号 | 含义 |
|---|---|
| $H_{ji}$ | 机柜 $j$ 的空气侧余热对机柜 $i$ 进风温度的热耦合系数 |
| $\beta_i$ | 有效送风冷量对机柜 $i$ 进风温度的抑制系数 |
| $T_i^{max}$ | 机柜 $i$ 允许的最高进风温度 |
| $T_{sup,z}^{old,0}$ | 老旧空调机组 $z$ 未老化时的基准送风温度 |
| $\Delta T_z^{age}$ | 老化导致的空调机组 $z$ 送风温度抬升 |
| $T_{sup}^{new}$ | 新空调的送风温度设定值 |
| $T_t^{out}$ | 时段 $t$ 的室外干球温度 |
| $T_t^{wb}$ | 时段 $t$ 的室外湿球温度 |
| $T_t^{chw,s}$ | 时段 $t$ 的冷冻水供水温度 |

### 5.6 设备效率、末端耗电与泵耗参数

| 符号 | 含义 |
|---|---|
| $e_z^{new,fan}$ | 新空调末端风机单位冷量耗电系数 |
| $e_z^{old,fan}$ | 老旧空调末端单位冷量耗电系数 |
| $e_z^{RDHX}$ | RDHX 背板整体单位冷量耗电系数，包含 RDHX 末端侧和循环泵等附属耗电，不再拆分 terminal 与 pump |
| $COP_t^{Chiller}$ | 冷机在时段 $t$ 的制冷 COP |
| $COP^{ASHP}$ | 空气源热泵 COP |
| $COP^{WSHP}$ | 水源热泵 COP |
| $e^{CDU,pump},e^{WSHP,pump}$ | CDU/冷板和水源热泵循环泵的单位热量输送耗电系数 |
| $e^{CDU,free}$ | 冷板/CDU 至冷却塔自然冷却支路的单位排热附属耗电系数，可包含自然冷却泵或板换侧附属耗电，但不含冷却塔风机耗电 |
| $e^{tower}$ | 冷却塔单位排热量风机耗电系数 |
| $\rho,g,H^{head},\eta_{pump}$ | 机理泵耗计算中的流体密度、重力加速度、泵扬程和泵效率 |
| $c_p,\Delta T$ | 水侧比热容和设计供回水温差，用于由热量换算流量 |

### 5.7 储能、供热与时间参数

| 符号 | 含义 |
|---|---|
| $Q_t^{heat,demand}$ | 外部供热需求 |
| $\Delta t$ | 单个运行时段长度 |
| $\mu^{TES}$ | 蓄热装置单位时段自损失率 |
| $\eta^{TES,ch},\eta^{TES,dis}$ | 蓄热充热效率和放热效率 |
| $\gamma^{TES}$ | 蓄热功率容量与热容量之间的倍率系数 |
| $\mu^{BESS}$ | 电池储能单位时段自放电率 |
| $\eta^{BESS,ch},\eta^{BESS,dis}$ | 电池充电效率和放电效率 |
| $\gamma^{BESS}$ | 电池储能整体容量对应的最大充放电功率倍率，体现固定储能时长或固定 C-rate |
| $\underline{soc},\overline{soc}$ | 电池允许的最低和最高荷电状态比例 |
| $N^{cycle}$ | 电池等效循环寿命 |
| $DoD^{max}$ | 电池最大放电深度 |

### 5.8 经济性、碳排与承重参数

| 符号 | 含义 |
|---|---|
| $Price_t^{grid}$ | 分时电价 |
| $cf_t^{grid}$ | 分时电网碳因子 |
| $Price^{heat}$ | 供热收益价格 |
| $cf^{boiler}$ | 被余热替代的传统供热碳排因子 |
| $C_t^{VOM}$ | 时段 $t$ 的可变运维成本项，可按设备运行量线性化 |
| $C^{ann}_{FOM}$ | 年化固定运维成本 |
| $\lambda^{hot}$ | 热安全松弛量惩罚成本系数 |
| $CRF_k$ | 构型 $k$ 固定改造投资的资本回收系数 |
| $CRF^{m}$ | 容量设备 $m$ 的资本回收系数，$m\in\{AC,RDHX,CDU,ASHP,WSHP,Chiller,TES,BESS,Tower\}$ |
| $c^{m}$ | 容量设备 $m$ 的单位容量投资成本 |
| $EI^{m}$ | 容量设备 $m$ 的单位容量隐含碳 |
| $n^{m}$ | 容量设备 $m$ 的折算寿命 |
| $W_i^{old}$ | 机柜 $i$ 改造前重量 |
| $w_i^{CP},w_i^{RD}$ | 机柜 $i$ 接入冷板或 RDHX 背板增加的重量 |
| $W_i^{rack,max}$ | 机柜 $i$ 允许的最大重量 |
| $W_{z,k}^{equip}$ | 空调机组区域 $z$ 选择构型 $k$ 时新增固定设备重量 |
| $w^{AC,new},w^{RDHX},w^{CDU}$ | 新空调、RDHX/CDU 的单位容量重量 |
| $Load_z^{floor,max}$ | 区域 $z$ 的楼板单位面积承载上限 |
| $Area_z$ | 区域 $z$ 的楼板承载核算面积 |

## 6. 规划层决策变量

### 6.1 构型选择变量

$$
u_{z,k}\in\{0,1\},\quad \forall z\in Z,\ k\in K
$$

$u_{z,k}=1$ 表示空调机组 $z$ 负责的机柜组统一采用构型 $k$。

唯一构型约束为：

$$
\sum_{k\in K}u_{z,k}=1,\quad \forall z\in Z
$$

### 6.2 构型派生状态变量

为简化表达，定义以下由 $u_{z,k}$ 派生得到的状态量：

$$
o_z=u_{z,0}+u_{z,2}+u_{z,4}
$$

$$
n_z=u_{z,1}+u_{z,3}+u_{z,6}
$$

$$
a_z=u_{z,2}+u_{z,3}+u_{z,4}+u_{z,6}
$$

$$
p_z=u_{z,4}+u_{z,6}
$$

$$
r_z=u_{z,5}
$$

$$
w_z=p_z+r_z=u_{z,4}+u_{z,5}+u_{z,6}
$$

其中：

- $o_z$ 表示保留老旧空调。
- $n_z$ 表示替换为新空调。
- $a_z$ 表示热通道空气余热可接入空气源热泵。
- $p_z$ 表示机柜组接入冷板。
- $r_z$ 表示机柜组采用背板式冷却。
- $w_z$ 表示水侧余热可接入水源热泵，包括冷板/CDU 侧热量和 RDHX 背板水侧热量。

### 6.3 容量规划变量

$$
Cap_z^{AC,new},\ Cap_z^{RDHX},\ Cap_z^{CDU}\ge 0,\quad \forall z\in Z
$$

其中 $Cap_z^{AC,new}$ 表示新空调末端容量，$Cap_z^{RDHX}$ 表示 RDHX 背板末端水侧冷却能力，$Cap_z^{CDU}$ 表示冷板构型下 CDU 的可用换热/输配容量。这些容量应作为规划决策变量，而不是固定参数，因为论文问题本身需要比较不同区域“是否改、改多大”的容量配置。RDHX 构型下不再单独设置 $Cap_z^{vent}$，因为背板式冷却按吸收该区域全部 IT 热量建模。

容量变量与构型选择之间的联动约束为：
$$
0\le Cap_z^{AC,new}\le \overline{Cap}_z^{AC,new} n_z,\quad \forall z\in Z
$$

$$
0\le Cap_z^{RDHX}\le \overline{Cap}_z^{RDHX} r_z,\quad \forall z\in Z
$$

$$
0\le Cap_z^{CDU}\le \overline{Cap}_z^{CDU} p_z,\quad \forall z\in Z
$$

公共设备容量规划变量为：
$$
Cap^{ASHP},\ Cap^{WSHP},\ Cap^{BESS},\ Cap^{TES}\ge 0
$$

分别表示空气源热泵容量、水源热泵容量、电池储能整体容量和蓄热容量。电池储能不再区分能量容量与功率容量；若运行调度需要充放电功率上限，由固定倍率 $\gamma^{BESS}$ 将整体容量换算为最大充放电功率。

若需要保留原模型的冷源和冷却塔扩容能力，可继续设置：

$$
\Delta Cap^{Chiller},\ \Delta Cap^{Tower}\ge 0
$$

总冷机和冷却塔能力为：

$$
Cap^{Chiller,tot}=Cap^{Chiller,old}+\Delta Cap^{Chiller}
$$

$$
Cap^{Tower,tot}=Cap^{Tower,old}+\Delta Cap^{Tower}
$$

## 7. 运行层变量

| 变量 | 含义 |
|---|---|
| $q_{i,t}^{IT}$ | 机柜 $i$ 在时段 $t$ 的 IT 热负荷 |
| $q_{i,t}^{liq}$ | 机柜 $i$ 由冷板或背板水侧带走的热负荷 |
| $q_{i,t}^{air}$ | 机柜 $i$ 仍需由空气侧处理的热负荷 |
| $s_{z,t}^{air}$ | 空调机组 $z$ 对其负责区域提供的有效空气侧冷却量，由老旧或新空调末端容量约束限制；RDHX 构型下取 0 |
| $Q_{z,t}^{CP}$ | 机组 $z$ 内由冷板/CDU 带走的热负荷 |
| $Q_{z,t}^{RDHX}$ | 机组 $z$ 内由 RDHX 背板带走的水侧热负荷 |
| $\theta_{i,t}$ | 机柜 $i$ 的进风温度代理 |
| $\xi_{i,t}$ | 机柜 $i$ 的热安全松弛量 |
| $Q_{z,t}^{air\to ASHP}$ | 机组 $z$ 热通道空气余热进入空气源热泵的热量 |
| $Q_{z,t}^{air\to reject}$ | 机组 $z$ 剩余需风冷排出的热量 |
| $Q_{z,t}^{CP\to WSHP}$ | 机组 $z$ 冷板/CDU 侧进入水源热泵的热量 |
| $Q_{z,t}^{CP\to free}$ | 机组 $z$ 冷板/CDU 侧进入自然冷却直接排热的热量 |
| $Q_{z,t}^{RDHX\to WSHP}$ | 机组 $z$ RDHX 水侧进入水源热泵的热量 |
| $Q_{z,t}^{RDHX\to Chiller}$ | 机组 $z$ RDHX 水侧未回收而由 Chiller 承担的冷负荷 |
| $Q_t^{CDU\to free}$ | 冷板/CDU 侧通过自然冷却直接排出的总热量，$Q_t^{CDU\to free}=\sum_z Q_{z,t}^{CP\to free}$ |
| $Q_t^{Chiller,evap}$ | 冷机蒸发侧需要承担的冷负荷 |
| $P_t^{Chiller}$ | 冷机耗电功率 |
| $P_t^{pump}$ | 冷板/CDU 和水源热泵循环泵耗电功率，不包含已并入 $e_z^{RDHX}$ 的 RDHX 附属耗电 |
| $P_t^{CDU,free}$ | 冷板/CDU 至冷却塔自然冷却支路的附属耗电功率，不含冷却塔风机耗电 |
| $Q_t^{Tower}$ | 冷却塔需要承担的总排热量，包括冷机冷凝侧排热和冷板/CDU 自然冷却直接排热 |
| $P_t^{tower}$ | 冷却塔风机耗电功率 |
| $P_t^{terminal}$ | 空调末端和 RDHX 整体附属耗电功率 |
| $Q_t^{ASHP,out}$ | 空气源热泵输出热量 |
| $Q_t^{WSHP,out}$ | 水源热泵输出热量 |
| $E_t^{TES}$ | 蓄热装置储热状态 |
| $Q_t^{TES,ch},Q_t^{TES,dis}$ | 蓄热充放热功率 |
| $E_t^{BESS}$ | 电池荷电状态 |
| $P_t^{BESS,ch},P_t^{BESS,dis}$ | 电池充放电功率 |
| $P_t^{grid}$ | 电网购电功率 |
| $P_t^{cooling}$ | 制冷系统总电功率 |

## 8. IT 负荷与冷热负荷分解

机柜 IT 负荷由外部曲线和业务爬升给定：

$$
q_{i,t}^{IT}=\widehat q_{i,t}^{IT},\quad \forall i\in R,\ t\in T
$$

若机柜 $i$ 属于空调机组 $z$ 负责的集合 $R_z$，则其液冷或背板水侧承担热负荷为：

$$
q_{i,t}^{liq}
=
\left(p_z\eta_i^{CP}+r_z\right)q_{i,t}^{IT},
\quad \forall i\in R_z,\ t\in T
$$

空气侧剩余热负荷为：

$$
q_{i,t}^{air}=q_{i,t}^{IT}-q_{i,t}^{liq},
\quad \forall i\in R,\ t\in T
$$

为避免把冷板/CDU 与 RDHX 背板的热量混入同一排热链，进一步定义机组级水侧热量分解：

$$
Q_{z,t}^{CP}
=
\sum_{i\in R_z}p_z\eta_i^{CP}q_{i,t}^{IT},
\quad \forall z\in Z,\ t\in T
$$

$$
Q_{z,t}^{RDHX}
=
\sum_{i\in R_z}r_zq_{i,t}^{IT},
\quad \forall z\in Z,\ t\in T
$$

$$
\sum_{i\in R_z}q_{i,t}^{liq}=Q_{z,t}^{CP}+Q_{z,t}^{RDHX},
\quad \forall z\in Z,\ t\in T
$$

该约束继承了原模型中风冷与液冷热负荷分解的思想，但不再对机柜内部设备组 $g$ 建立独立改造变量。

液冷渗透率可作为方案评价指标：

$$
\rho^{LC}
=
\frac{\sum_{t\in T}\sum_{i\in R}q_{i,t}^{liq}}
{\sum_{t\in T}\sum_{i\in R}q_{i,t}^{IT}}
$$

也可以采用机柜数量口径：

$$
\rho_{rack}^{LC}
=
\frac{\sum_{z\in Z}(p_z+r_z)|R_z|}{|R|}
$$

论文中建议优先使用热负荷口径 $\rho^{LC}$，因为其能反映高功率机柜对液冷改造价值的影响。

## 9. 热安全与机柜热耦合约束

对机柜 $i\in R_z$，进风温度代理为：

$$
\theta_{i,t}
=
T_{sup,z,t}
+
\sum_{j\in R}H_{ji}q_{j,t}^{air}
-
\beta_i s_{z,t}^{air},
\quad \forall i\in R_z,\ t\in T
$$

其中 $s_{z,t}^{air}$ 表示空调机组 $z$ 对其负责区域提供的有效空气侧冷却量。它不是由空气侧剩余热负荷强制等式确定，而是作为热安全约束中的运行变量，由该区域老旧或新空调末端容量给出上界。RDHX 构型下由于背板承担 100% IT 热量，空气侧热负荷和有效空气侧冷却量均不再单独规划：

$$
0\le s_{z,t}^{air}
\le
o_z Cap_{z,t}^{old,eff}
+
n_z Cap_z^{AC,new},
\quad \forall z\in Z,\ t\in T
$$

空气侧热量分流仍由第 10 节中的 $\sum_{i\in R_z}q_{i,t}^{air}=Q_{z,t}^{air\to ASHP}+Q_{z,t}^{air\to reject}$ 描述；$s_{z,t}^{air}$ 只表示空调侧能够提供的有效局部降温能力。为避免 $s_{z,t}^{air}$ 成为无成本的自由降温变量，末端风机耗电应按 $s_{z,t}^{air}$ 计入运行成本。若后续采用更细的气流组织模型，也可将 $s_{z,t}^{air}$ 改写为送风量、送回风温差和局部供冷有效性的函数。热安全约束为：

$$
\theta_{i,t}\le T_i^{max}+\xi_{i,t},
\quad \forall i\in R,\ t\in T
$$

$$
\xi_{i,t}\ge 0,\quad \forall i\in R,\ t\in T
$$

为了体现老旧空调老化，可将送风温度写为：

$$
T_{sup,z,t}
=
o_z\left(T_{sup,z}^{old,0}+\Delta T_z^{age}\right)
+
n_zT_{sup}^{new}
$$

其中 $\Delta T_z^{age}$ 表示因老化导致的送风温度抬升。若暂时缺少送风温度实测数据，可仅使用老化后的容量约束，并将 $T_{sup,z,t}$ 作为情景参数。

## 10. 风冷、RDHX 与 Chiller 冷源约束

空气侧热负荷在构型允许时可被空气源热泵回收，其余部分由老旧或新空调末端处理。RDHX 构型下由于背板承担该区域全部 IT 热量，$q_{i,t}^{air}=0$，因此不再引入辅助风冷/房间冷却链路：

$$
\sum_{i\in R_z}q_{i,t}^{air}
=
Q_{z,t}^{air\to ASHP}
+
Q_{z,t}^{air\to reject},
\quad \forall z\in Z,\ t\in T
$$

空气源热泵只有在构型允许时才可接入：

$$
Q_{z,t}^{air\to ASHP}
\le
a_z\sum_{i\in R_z}q_{i,t}^{air},
\quad \forall z\in Z,\ t\in T
$$

空调末端能力约束为：

$$
Q_{z,t}^{air\to reject}
\le
o_z Cap_{z,t}^{old,eff}
+
n_z Cap_z^{AC,new},
\quad \forall z\in Z,\ t\in T
$$

其中老旧空调容量是既有设备参数，新空调容量 $Cap_z^{AC,new}$ 是规划变量；RDHX 构型下不再设置辅助风冷/房间冷却容量。

RDHX 背板承担的水侧热负荷还需要满足末端容量约束：
$$
Q_{z,t}^{RDHX}
\le
Cap_z^{RDHX},
\quad \forall z\in Z,\ t\in T
$$

冷板构型下，CDU 容量限制冷板回路能够从机柜侧带走的热负荷。该热量随后可进入水源热泵或自然冷却排热侧：

$$
Q_{z,t}^{CP}
\le
Cap_z^{CDU},
\quad \forall z\in Z,\ t\in T
$$

总空气源热泵输入热量为：

$$
Q_t^{ASHP,in}=\sum_{z\in Z}Q_{z,t}^{air\to ASHP}
$$

空气源热泵输出与耗电功率为：

$$
Q_t^{ASHP,out}
=
Q_t^{ASHP,in}\frac{COP^{ASHP}}{COP^{ASHP}-1}
$$

$$
P_t^{ASHP}
=
\frac{Q_t^{ASHP,out}}{COP^{ASHP}}
$$

容量约束为：

$$
Q_t^{ASHP,out}\le Cap^{ASHP},\quad \forall t\in T
$$

只有空调末端和 RDHX 背板需要 Chiller 作为冷源。空气侧未被空气源热泵回收的部分，以及 RDHX 未进入水源热泵的部分，构成冷机蒸发侧负荷；冷板/CDU 负荷无论进入水源热泵还是自然冷却侧，均不计入 Chiller：

$$
Q_t^{Chiller,evap}
=
\sum_{z\in Z}Q_{z,t}^{air\to reject}
+
\sum_{z\in Z}Q_{z,t}^{RDHX\to Chiller}
$$

冷机容量和耗电功率约束为：
$$
Q_t^{Chiller,evap}
\le
\frac{Cap^{Chiller,old}+\Delta Cap^{Chiller}}{1+\gamma^{Chiller}},
\quad \forall t\in T
$$

$$
P_t^{Chiller}
=
\frac{Q_t^{Chiller,evap}}{COP_t^{Chiller}},
\quad \forall t\in T
$$

## 11. 冷板/CDU、RDHX 余热回收与排热约束

冷板侧热量经 CDU 后在水源热泵回收链和自然冷却直接排热链之间分配：

$$
Q_{z,t}^{CP}
=
Q_{z,t}^{CP\to WSHP}
+
Q_{z,t}^{CP\to free},
\quad \forall z\in Z,\ t\in T
$$

冷板/CDU 侧热量只有在冷板构型下才能进入水源热泵：

$$
Q_{z,t}^{CP\to WSHP}
\le
Q_{z,t}^{CP},
\quad \forall z\in Z,\ t\in T
$$

进入冷却塔的冷板/CDU 自然冷却直接排热量及其支路附属耗电为：

$$
Q_t^{CDU\to free}
=
\sum_{z\in Z}Q_{z,t}^{CP\to free},
\quad \forall t\in T
$$

$$
P_t^{CDU,free}
=
e^{CDU,free}Q_t^{CDU\to free},
\quad \forall t\in T
$$

RDHX 背板水侧热量则在水源热泵回收链和 Chiller 冷源链之间分配：

$$
Q_{z,t}^{RDHX}
=
Q_{z,t}^{RDHX\to WSHP}
+
Q_{z,t}^{RDHX\to Chiller},
\quad \forall z\in Z,\ t\in T
$$

RDHX 水侧余热只有在背板构型下才能进入水源热泵：

$$
Q_{z,t}^{RDHX\to WSHP}
\le
Q_{z,t}^{RDHX},
\quad \forall z\in Z,\ t\in T
$$

水源热泵输入热量为：

$$
Q_t^{WSHP,in}
=
\sum_{z\in Z}
\left(
Q_{z,t}^{CP\to WSHP}
+
Q_{z,t}^{RDHX\to WSHP}
\right)
$$

水源热泵输出与耗电功率为：

$$
Q_t^{WSHP,out}
=
Q_t^{WSHP,in}\frac{COP^{WSHP}}{COP^{WSHP}-1}
$$

$$
P_t^{WSHP}
=
\frac{Q_t^{WSHP,out}}{COP^{WSHP}}
$$

容量约束为：

$$
Q_t^{WSHP,out}\le Cap^{WSHP},\quad \forall t\in T
$$

冷却塔同时承担冷机冷凝侧排热，以及冷板/CDU 中未进入水源热泵的自然冷却直接排热。简化情况下可写为：

$$
Q_t^{Tower}
=
Q_t^{Chiller,evap}
+
P_t^{Chiller}
+
Q_t^{CDU\to free}
$$

并约束：

$$
Q_t^{Tower}
\le
\frac{Cap^{Tower,old}+\Delta Cap^{Tower}}{1+\gamma^{Tower}}
$$

若显式计入冷却塔风机耗电，可采用线性近似：

$$
P_t^{tower}=e^{tower}Q_t^{Tower},\quad \forall t\in T
$$

## 12. 蓄热与供热平衡

蓄热状态转移为：

$$
E_t^{TES}
=
(1-\mu^{TES})E_{t-1}^{TES}
+
\left(\eta^{TES,ch}Q_t^{TES,ch}
-
\frac{Q_t^{TES,dis}}{\eta^{TES,dis}}\right)\Delta t
$$

容量约束为：

$$
0\le E_t^{TES}\le Cap^{TES},\quad \forall t\in T
$$

充放热功率约束为：

$$
0\le Q_t^{TES,ch}\le \gamma^{TES}Cap^{TES}\delta_t^{TES,ch}
$$

$$
0\le Q_t^{TES,dis}\le \gamma^{TES}Cap^{TES}\delta_t^{TES,dis}
$$

$$
\delta_t^{TES,ch}+\delta_t^{TES,dis}\le 1
$$

供热平衡为：

$$
Q_t^{ASHP,out}
+
Q_t^{WSHP,out}
+
Q_t^{TES,dis}
=
Q_t^{heat,supply}
+
Q_t^{TES,ch}
$$

供热需求约束为：

$$
Q_t^{heat,supply}\ge Q_t^{heat,demand},\quad \forall t\in T
$$

## 13. 电池储能与电力平衡

电池状态转移为：

$$
E_t^{BESS}
=
(1-\mu^{BESS})E_{t-1}^{BESS}
+
\left(\eta^{BESS,ch}P_t^{BESS,ch}
-
\frac{P_t^{BESS,dis}}{\eta^{BESS,dis}}\right)\Delta t
$$

SOC 约束为：

$$
\underline{soc}Cap^{BESS}
\le
E_t^{BESS}
\le
\overline{soc}Cap^{BESS}
$$

充放电功率约束为。由于 BESS 不再区分功率容量和能量容量，模型采用固定倍率 $\gamma^{BESS}$ 将整体容量换算为可用充放电功率：

$$
0\le P_t^{BESS,ch}\le \gamma^{BESS}Cap^{BESS}\delta_t^{BESS,ch}
$$

$$
0\le P_t^{BESS,dis}\le \gamma^{BESS}Cap^{BESS}\delta_t^{BESS,dis}
$$

$$
\delta_t^{BESS,ch}+\delta_t^{BESS,dis}\le 1
$$

制冷与热泵总电功率为：

$$
P_t^{cooling}
=
P_t^{Chiller}
+
P_t^{ASHP}
+
P_t^{WSHP}
+
P_t^{pump}
+
P_t^{CDU,free}
+
P_t^{terminal}
+
P_t^{tower}
$$

其中 $P_t^{terminal}$ 表示空调末端和 RDHX 的附属耗电。由于当前数据只能拟合 RDHX 整体单位冷量耗电系数，本文将 RDHX 末端侧与循环泵耗电合并为 $e_z^{RDHX}$，不再拆分为末端侧和循环泵两个耗电项：

$$
P_t^{terminal}
=
\sum_{z\in Z}
\left[
e_z^{old,fan}o_zs_{z,t}^{air}
+
e_z^{new,fan}n_zs_{z,t}^{air}
+
e_z^{RDHX}Q_{z,t}^{RDHX}
\right]
$$

$P_t^{pump}$ 表示未并入设备整体耗电系数的水侧循环泵功率，不是热泵压缩机功率。由于 RDHX 循环泵耗电已计入 $e_z^{RDHX}Q_{z,t}^{RDHX}$，$P_t^{pump}$ 只保留 CDU/冷板回路和 WSHP 取热回路：

$$
P_t^{pump}
=
e^{CDU,pump}\sum_{z\in Z}Q_{z,t}^{CP}
+
e^{WSHP,pump}Q_t^{WSHP,in}
$$

若需要从流量机理计算，CDU/冷板回路和 WSHP 取热回路均可由 $P=\rho gH^{head}\dot V/\eta_{pump}$ 得到，其中 $\dot V=Q/(c_p\Delta T)$。对于 RDHX，论文主模型建议直接使用由数据拟合得到的整体单位冷量耗电系数 $e_z^{RDHX}$，以保持内层调度问题的线性结构并匹配数据可得性。

总电力平衡为：

$$
P_t^{grid}
+
P_t^{BESS,dis}
=
\sum_{i\in R}q_{i,t}^{IT}
+
P_t^{cooling}
+
P_t^{BESS,ch}
$$

## 14. 承重约束

本文不再考虑机房空间面积约束，仅保留承重可行性约束。机柜改造后的重量为：

$$
W_i^{new}
=
W_i^{old}
+
\sum_{z:i\in R_z}\left(p_z w_i^{CP}+r_z w_i^{RD}\right)
$$

$$
W_i^{new}\le W_i^{rack,max},\quad \forall i\in R
$$

区域楼板承载约束为：

$$
\sum_{i\in R_z}W_i^{new}
+
\sum_{k\in K}W_{z,k}^{equip}u_{z,k}
+
w^{AC,new}Cap_z^{AC,new}
+
w^{RDHX}Cap_z^{RDHX}
+
w^{CDU}Cap_z^{CDU}
\le
Load_z^{floor,max}Area_z,
\quad \forall z\in Z
$$

其中 $W_{z,k}^{equip}$ 表示构型固定设备重量，$w^{AC,new}$、$w^{RDHX}$ 和 $w^{CDU}$ 分别表示新空调、RDHX 背板末端和 CDU 的单位容量重量。若设备重量数据不足，可先将其作为情景参数或保守上界。

## 15. 目标函数一：最小化年化总成本

年化总成本为：

$$
\min F_{cost}
=
C^{ann}_{retro}
+
C^{ann}_{cap}
+
C^{ann}_{FOM}
+
\sum_{t\in T}
\left(
Price_t^{grid}P_t^{grid}
-
Price^{heat}Q_t^{heat,supply}
+
C_t^{VOM}
+
\lambda^{hot}\sum_{i\in R}\xi_{i,t}
\right)
$$

构型改造投资年化为：

$$
C^{ann}_{retro}
=
\sum_{z\in Z}\sum_{k\in K}
CRF_k C_{z,k}^{retro}u_{z,k}
$$

为避免重复计算，本文采用“固定改造项”和“容量设备项”分账口径：$C_{z,k}^{retro}$ 只表示选择构型 $k$ 后必然发生、且不随规划容量连续变化的一次性固定改造成本；新空调、RDHX、CDU、热泵、冷机、冷却塔和储能等新增或扩容设备的投资，统一进入 $C^{ann}_{cap}$。若数据来源给出的是包含设备采购的总包报价，应先扣除可按容量计价的设备部分，或取消相应容量设备项，二者不能同时计入。

容量设备投资年化为：

$$
C^{ann}_{cap}
=
\sum_{z\in Z}
\left(
CRF^{AC}c^{AC,new}Cap_z^{AC,new}
+
CRF^{RDHX}c^{RDHX}Cap_z^{RDHX}
+
CRF^{CDU}c^{CDU}Cap_z^{CDU}
\right)
+
CRF^{ASHP}c^{ASHP}Cap^{ASHP}
+
CRF^{WSHP}c^{WSHP}Cap^{WSHP}
+
CRF^{Chiller}c^{Chiller}\Delta Cap^{Chiller}
$$

$$
+
CRF^{TES}c^{TES}Cap^{TES}
+
CRF^{BESS}c^{BESS}Cap^{BESS}
+
C^{deg}_{BESS}
+
CRF^{Tower}c^{Tower}\Delta Cap^{Tower}
$$

其中电池寿命折损继承原模型的按吞吐量折旧口径：

$$
E_{life}^{BESS}
=
Cap^{BESS}N^{cycle}DoD^{max}\eta^{BESS,dis}
$$

$$
c_{deg}^{BESS}
=
\frac{Cap^{BESS}c^{BESS}}{E_{life}^{BESS}}
$$

$$
C^{deg}_{BESS}
=
\sum_{t\in T}c_{deg}^{BESS}P_t^{BESS,dis}
$$

## 16. 目标函数二：最小化年化碳排放

年化碳排放为：

$$
\min F_{carbon}
=
E^{ann}_{emb}
+
\sum_{t\in T}
\left(
cf_t^{grid}P_t^{grid}
-
cf^{boiler}Q_t^{heat,supply}
\right)
$$

构型隐含碳年化为：

$$
E^{ann}_{retro}
=
\sum_{z\in Z}\sum_{k\in K}
\frac{EI_{z,k}^{retro}}{n_k}u_{z,k}
$$

隐含碳采用与成本相同的分账口径：$EI_{z,k}^{retro}$ 仅表示构型固定改造本身的隐含碳，容量设备制造、运输和安装对应的隐含碳统一由 $E^{ann}_{cap}$ 中的 $EI^m Cap^m/n^m$ 表示。若 $EI_{z,k}^{retro}$ 的数据来自包含设备的总量清单，也需要剔除已在容量设备项中计入的部分。

容量设备隐含碳年化为：

$$
E^{ann}_{cap}
=
\sum_{z\in Z}
\left(
\frac{EI^{AC,new}Cap_z^{AC,new}}{n^{AC}}
+
\frac{EI^{RDHX}Cap_z^{RDHX}}{n^{RDHX}}
+
\frac{EI^{CDU}Cap_z^{CDU}}{n^{CDU}}
\right)
+
\frac{EI^{ASHP}Cap^{ASHP}}{n^{ASHP}}
+
\frac{EI^{WSHP}Cap^{WSHP}}{n^{WSHP}}
+
\frac{EI^{Chiller}\Delta Cap^{Chiller}}{n^{Chiller}}
$$

$$
+
\frac{EI^{TES}Cap^{TES}}{n^{TES}}
+
\frac{EI^{BESS}Cap^{BESS}}{n^{BESS}}
+
E^{deg}_{BESS}
+
\frac{EI^{Tower}\Delta Cap^{Tower}}{n^{Tower}}
$$

电池使用隐含碳折损为：

$$
ei_{deg}^{BESS}
=
\frac{Cap^{BESS}EI^{BESS}}{E_{life}^{BESS}}
$$

$$
E^{deg}_{BESS}
=
\sum_{t\in T}ei_{deg}^{BESS}P_t^{BESS,dis}
$$

总隐含碳为：

$$
E^{ann}_{emb}=E^{ann}_{retro}+E^{ann}_{cap}
$$

## 17. 多目标优化问题

综合上述目标与约束，新问题可写为：

$$
\min
\left(
F_{cost},
F_{carbon}
\right)
$$

决策变量包括：

$$
\{u_{z,k}\}_{z\in Z,k\in K},
\{Cap_z^{AC,new},Cap_z^{RDHX},Cap_z^{CDU}\}_{z\in Z},
Cap^{ASHP},
Cap^{WSHP},
Cap^{BESS},
Cap^{TES},
\Delta Cap^{Chiller},
\Delta Cap^{Tower}
$$

以及运行层变量：

$$
\{q^{air},q^{liq},Q^{CP},Q^{RDHX},Q^{air\to ASHP},Q^{CP\to WSHP},Q^{CP\to free},Q^{RDHX\to WSHP},Q^{RDHX\to Chiller},Q^{CDU\to free},Q^{Chiller,evap},E^{TES},E^{BESS},P^{grid},\dots\}
$$

该问题保留混合整数、多目标和运行调度耦合特征。建议继续沿用原项目的外层 NSGA-II 与内层 MILP 评估框架。

## 18. 求解框架继承与修改

### 18.1 外层规划层

原模型外层变量为：

$$
y_i,\ x_{ig},\ N_{CRAC}^{remove},\ Cap_{CDU},\ Cap_{HP},\ Cap_{BESS}^{E},\ Cap_{BESS}^{P},\ Cap_{TES},\Delta Cap_{Tower},Cap_{HX}
$$

新模型建议替换为：

$$
u_{z,k},\ Cap_z^{AC,new},\ Cap_z^{RDHX},\ Cap_z^{CDU},\ Cap^{ASHP},\ Cap^{WSHP},\ Cap^{BESS},\ Cap^{TES},\Delta Cap^{Chiller},\Delta Cap^{Tower}
$$

其中 $u_{z,k}$ 是 one-hot 构型变量。实现时可用整数变量 $s_z\in\{0,\dots,6\}$ 表示构型，再在解码阶段转换为 $u_{z,k}$。

### 18.2 内层运行层

内层继续求解运行调度模型，输入为外层给定的构型与容量。内层返回：

- 年运行购电成本。
- 年运行碳排。
- 供热收益。
- 热安全违约惩罚。
- 储能调度结果。
- 关键设备利用率。

这些运行结果与外层投资、固定运维和隐含碳合并，形成两个目标函数值，回传给 NSGA-II。

### 18.3 代码层需要修改的位置

原代码中与新模型冲突最大的部分是规划解码：

- `model.py` 中 `get_decision_schema()` 当前按机柜生成 $y_i$，按机柜和设备组生成 $x_{ig}$。
- `model.py` 中 `_decode_topology()` 当前将外层向量解码为 $y_i$、$x_{ig}$ 和 `n_crac_remove`。
- `inner_dispatch.py` 中 `FixedPlanningDecision` 当前保存的是机柜级液冷改造决策。

新实现应改为：

- 在 `DecisionSchema` 中生成 $s_z$ 或 $u_{z,k}$。
- 在解码阶段根据构型生成 $o_z,n_z,a_z,p_z,r_z,w_z$。
- 在连续容量变量中加入 $Cap_z^{CDU}$，并仅在 $p_z=1$ 的冷板构型下允许其取正。
- 将 `FixedPlanningDecision` 改为保存构型数组、新空调/RDHX/CDU 容量、热泵容量、BESS 整体容量、TES 容量和 Chiller 扩容容量。
- 用 RF 拟合得到的 $R_z$ 替代原代码中的随机 `zone_of_rack`。
- 将原来的 `q_lc = q_it * sum(eta_ig*x_ig*lambda_ig)` 改为基于构型的 `q_liq = q_it*(p_z*eta_cp + r_z)`，并进一步拆分为 `q_cp = q_it*p_z*eta_cp` 与 `q_rdhx = q_it*r_z`；其中 RDHX 按吸收该区域 100% IT 热量建模。
- 在热量流逻辑中将 `q_cp` 接入 CDU 后再分配到“水源热泵回收或自然冷却直接排热”两条链，将 `q_rdhx` 接入“水源热泵回收或 Chiller 冷源”分配链；Chiller 负荷只汇总空气侧未回收热量和 RDHX 未回收热量。

## 19. 实验方案

### 19.1 Baseline 1：不改造

所有空调机组均选择构型 0：

$$
u_{z,0}=1,\quad \forall z\in Z
$$

同时：

$$
Cap_z^{CDU}=0,\quad \forall z\in Z
$$

$$
Cap^{ASHP}=Cap^{WSHP}=Cap^{TES}=Cap^{BESS}=\Delta Cap^{Chiller}=\Delta Cap^{Tower}=0
$$

该方案用于评价老旧风冷系统在业务负荷爬升和空调老化背景下的热安全风险、运行成本和碳排放。

### 19.2 Baseline 2：整体激进改造

所有空调机组均选择构型 6：

$$
u_{z,6}=1,\quad \forall z\in Z
$$

即老旧空调全部替换为新空调，同时机柜接入冷板，热通道接空气源热泵，冷板经 CDU 后可进入水源热泵或自然冷却侧直接排热。容量变量可继续由优化模型确定，也可按峰值负荷进行工程 sizing。为了与本文方案公平比较，建议仍然优化新空调容量、CDU 容量、空气源热泵容量、水源热泵容量、Chiller 扩容、蓄热和电储能容量。

### 19.3 本文方案：精细化改造

每个空调机组 $z$ 独立选择 7 类构型之一：

$$
\sum_{k\in K}u_{z,k}=1,\quad \forall z\in Z
$$

模型同时优化新空调容量、CDU 容量、RDHX 背板容量、空气源热泵、水源热泵、Chiller 扩容、蓄热和电池储能整体容量，输出 Pareto 前沿。该方案能够识别“哪些空调机组负责的机柜组更值得采用冷板并回收余热、哪些冷板区域更适合自然冷却排热、哪些区域适合 RDHX 余热回收、哪些区域仅替换新空调即可”的差异。

## 20. 论文中可强调的创新点

1. 从机柜级离散改造转向空调机组负责区域的统一构型选择，更贴近既有数据中心实际改造组织方式。
2. 将空调老化和智算业务负荷爬升同时纳入规划模型，解释为什么老旧风冷中心在转型过程中会出现热安全压力。
3. 将热通道空气余热、冷板/CDU 水侧余热、RDHX 水侧余热和自然冷却排热分开建模，明确冷板热量可由水源热泵回收，也可自然冷却排热，但不进入 Chiller，并取消液冷侧直接供热假设。
4. 在同一模型中协同优化液冷渗透率、新空调/CDU/RDHX 容量、空气源/水源热泵容量、蓄热容量、电储能整体容量和既有冷源基础设施调整。
5. 通过年化成本和年化碳排放 Pareto 前沿展示低碳改造中的经济性和减排性权衡。

## 21. 建模口径小结

新模型继承原模型的运行调度、电热耦合、热安全和 Pareto 双目标框架，但将规划层改造粒度调整为“空调机组负责的机柜组”。因此，论文建模时不应再强调单个机柜是否改造或设备组是否接入冷板，而应强调不同空调机组区域在老化程度、业务负荷爬升、热点风险和余热价值上的差异，并由构型选择变量 $u_{z,k}$ 决定每个区域采用何种低碳改造方案。
