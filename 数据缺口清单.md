# 数据缺口清单

本文档根据 `data` 文件夹当前文件，对照 `论文研究问题_建模.md` 中的模型参数与约束，整理已具备数据、可派生数据和仍需补充的数据。

## 当前已有数据

| 数据文件 | 覆盖内容 | 可对应模型项 | 备注 |
|---|---|---|---|
| `data/time_series.csv` | 49 个机柜逐小时 IT 负荷，20162 行，时间范围 2020-03-09 11:00:00 至 2022-09-28 22:00:00 UTC | $\widehat q_{i,t}^{0}$、$q_{i,t}^{IT}$、总 IT 负荷 | 可作为主负荷曲线；若研究 2025 年电价/碳因子，可按小时序列或典型日映射 |
| `data/rack_metadata.csv` | 49 个机柜元数据，包含区域、行列位置、服务器类型混合、热点风险、容量代理 | $R_z$、机柜空间位置、$Q_i^{IT,max}$ 的代理值 | 已有 3 个区域：`room_y_2`、`room_y_6`、`room_y_10` |
| `data/H_matrix.csv` | 49 x 49 热耦合矩阵 | $H_{ji}$ | 行列数与机柜数一致，无表头 |
| `data/weather_open_meteo_hourly.csv` | 室外干球温度、露点温度，22403 行，时间范围 2020-03-09 12:00:00 至 2022-09-28 22:00:00 UTC | $T_t^{out}$；可近似派生湿球温度 | 缺少直接湿球温度和冷冻水供水温度；起始时间比负荷晚 1 小时 |
| `data/price_grid_beijing_2025_two_part_35_110kv_hourly_yuan_per_kwh.csv` | 北京 2025 年逐小时工商业分时电价，8760 行 | $Price_t^{grid}$ | 与 2020-2022 负荷不在同一年，可按小时序列或典型年方式映射 |
| `data/carbon_factor_China_2025_kgCO2_per_kWh.csv` | 2025 年逐小时省级电网碳因子，8760 行 | $cf_t^{grid}$ | 可选北京列或全国列；与负荷年份不同，需要统一时间映射口径 |
| `data/m100_22-07_vertiv_ac_metrics.csv` | Vertiv 空调运行数据，文件很大；抽样包含 `cdz1` 至 `cdz6` 的 `Return_Air_Temperature` | 可辅助拟合空调温度/老化相关参数 | 需进一步确认是否包含送风温度、风机功率、冷量或压缩机功率等字段 |

## 已记录拟合参数

| 参数 | 当前取值 | 文件 |
|---|---:|---|
| $e_z^{RDHX}$ | 0.0265 kWe/kWth | `模型参数取值.md` |
| $COP_t^{Chiller}$ | 3 | `模型参数取值.md` |
| $e_z^{old,fan}$ | 0.2137 kWe/kWth | `模型参数取值.md` |
| $e_z^{new,fan}$ | 0.2137 kWe/kWth | `模型参数取值.md` |

## 可由现有数据派生的数据

| 待用参数 | 派生方式 | 注意事项 |
|---|---|---|
| $Q_i^{IT,max}$ | 可用 `rack_metadata.csv` 中 `cap_slot_kw_proxy` 或 `branch_cap_kw_proxy` 作为代理 | 需要明确采用哪一个容量代理 |
| $\rho_i^{load}$ | 可按服务器类型混合、热点风险或设定情景生成 | 当前没有真实未来业务增长数据 |
| $\tau_t$ 或 $Y_t$ | 可由规划年限或时间索引构造 | 需要确定是线性爬升还是指数爬升 |
| $T_t^{wb}$ | 可由干球温度和露点温度近似计算 | 不是原始观测湿球温度 |
| $\psi_t^{amb}$ | 可先取 1，也可由天气和厂家曲线拟合 | 当前缺少厂家容量修正曲线 |

## 仍需补充的关键数据

## 结合 `原研究问题/可参考数据.md` 后的状态

`原研究问题/可参考数据.md` 可以作为方案阶段默认参数来源，尤其适合补充投资、隐含碳、机柜重量、CDU 重量和楼板承重等参数。但它不能替代现场容量台账、运行校准数据和供热需求数据。

### 可由参考数据先行补充的参数

| 当前模型项 | 可参考数据来源 | 建议用法 |
|---|---|---|
| $c^{CDU}$ | `capex.cdu_yuan_per_kw`，默认 1300 元/kW | 可作为 CDU 单位容量投资 |
| $c^{ASHP}$、$c^{WSHP}$ | `capex.hp_yuan_per_kw`，默认 1200 元/kW | 可先对空气源/水源热泵共用，后续按机型区分 |
| $c^{TES}$ | `capex.tes_yuan_per_kwh`，默认 180 元/kWh_th | 适用于水蓄热/水蓄冷型显热储能 |
| $c^{Tower}$ | `capex.tower_yuan_per_kw`，默认 220 元/kW | 置信度低，需做敏感性分析 |
| $c^{BESS}$ | `capex.bess_yuan_per_kwh` 或由原能量/功率成本按固定储能时长折算 | 本文将 BESS 作为整体容量设备，成本不再拆分为能量项和功率项 |
| $EI^{CDU}$ | `embodied.cdu_kg_per_kw`，默认 5 kgCO2e/kW | 可作为 CDU 单位容量隐含碳 |
| $EI^{ASHP}$、$EI^{WSHP}$ | `embodied.hp_kg_per_kw`，默认 12 kgCO2e/kW | 可先对空气源/水源热泵共用 |
| $EI^{TES}$ | `embodied.tes_kg_per_kwh`，默认 10 kgCO2e/kWh_th | 置信度低，建议敏感性分析 |
| $EI^{Tower}$ | `embodied.tower_kg_per_kw`，默认 5 kgCO2e/kW | 可作为冷却塔单位容量隐含碳 |
| $EI^{BESS}$ | `embodied.bess_kg_per_kwh` 或由原能量/功率隐含碳按固定储能时长折算 | 本文将 BESS 隐含碳按整体容量计，不再拆分为能量项和功率项 |
| $W_i^{old}$ | 原机柜重量，默认 150 kg/柜 | 可作为机柜改造前重量 |
| $w_i^{CP}$ | 液冷改造新增重量 50 kg/柜 + 冷板新增重量 1.25 kg/kW_IT | 建议拆成固定项和按液冷 IT 功率变化项 |
| $W_i^{rack,max}$ | 最大机柜静载，默认 1500 kg/柜 | 可作为机柜静载约束 |
| $Load_z^{floor,max}$ | 主机房楼面承载默认 1000 kg/m² | 当前模型不考虑空间约束，但仍可用于承重约束 |
| $w^{CDU}$ | CDU weight coefficient，默认 1.5 kg/kW | 可作为 CDU 单位容量重量 |
| $\eta_i^{RD}$ | 按资料调研取 1 | RDHX 基本吸收 100% IT 热量，因此不再作为待拟合分流比例 |

### 仍需真实项目数据或另行假设的参数

| 仍缺数据 | 原因 |
|---|---|
| $Cap_z^{old}$ | 参考数据没有给出本项目每个空调区域的既有空调容量 |
| $Cap^{Chiller,old}$ | 参考数据没有给出本项目既有冷机容量 |
| $Cap^{Tower,old}$ | 参考数据只给单位投资/隐含碳，未给既有冷却塔容量 |
| $\overline{Cap}_z^{AC,new}$、$\overline{Cap}_z^{RDHX}$、$\overline{Cap}_z^{CDU}$ | 需要由现场安装条件、设备选型或容量上限情景确定 |
| $c^{AC,new}$、$EI^{AC,new}$、$w^{AC,new}$ | 参考数据没有直接给新空调或 CRAC 新购设备的完整单位容量成本、隐含碳和重量 |
| $c^{RDHX}$、$EI^{RDHX}$、$w^{RDHX}$ | 参考数据主要覆盖冷板/CDU，不足以直接给 RDHX 背板设备成本、隐含碳和重量 |
| $c^{Chiller}$、$EI^{Chiller}$ | 参考数据没有给 Chiller 扩容的单位投资和隐含碳 |
| $C_{z,k}^{retro}$、$EI_{z,k}^{retro}$ | 参考数据可提供机柜液冷固定改造、冷板、管路和停机迁移默认值，但还需要把它们按当前 7 类构型重新汇总，避免与容量设备重复计算 |
| $n_k$、$n^m$、$CRF_k$、$CRF^m$ | 参考数据未系统给出设备寿命和折现率，需要统一年化口径 |
| $\eta_i^{CP}$ | 参考数据没有给冷板对 IT 热量的分流比例，需要由技术假设或文献/厂家参数给出；RDHX 比例已取 1 |
| $\beta_i$、$T_i^{max}$、$T_{sup,z}^{old,0}$、$\Delta T_z^{age}$、$T_{sup}^{new}$ | 热安全校准参数仍需由运行数据、ASHRAE/设备规范或情景假设确定 |
| $COP^{ASHP}$、$COP^{WSHP}$ | 参考数据给的是热泵投资，不是热泵运行 COP |
| $e^{CDU,pump}$、$e^{WSHP,pump}$、$e^{CDU,free}$、$e^{tower}$ | 参考数据没有给运行耗电系数 |
| $Q_t^{heat,demand}$、$Price^{heat}$、$cf^{boiler}$ | 供热侧需求、收益和替代碳因子仍需另行给定 |
| $C^{ann}_{FOM}$、$C_t^{VOM}$ | 参考数据主要是 CAPEX 和隐含碳，缺少运维成本参数 |
| $Area_z$ | 楼板承重约束需要区域核算面积；参考数据只给承载强度默认值 |

### 规划容量与既有设备

| 缺失数据 | 对应模型项 |
|---|---|
| 每个区域既有空调额定制冷容量 | $Cap_z^{old}$ |
| 既有 Chiller 可用制冷容量 | $Cap^{Chiller,old}$ |
| 既有冷却塔可用排热容量 | $Cap^{Tower,old}$ |
| 新空调、RDHX、CDU 的区域可安装容量上限 | $\overline{Cap}_z^{AC,new}$、$\overline{Cap}_z^{RDHX}$、$\overline{Cap}_z^{CDU}$ |
| Chiller 与冷却塔容量安全裕度 | $\gamma^{Chiller}$、$\gamma^{Tower}$ |

### 改造构型与液冷比例

| 缺失数据 | 对应模型项 |
|---|---|
| 各构型固定改造成本 | $C_{z,k}^{retro}$ |
| 各构型固定改造隐含碳 | $EI_{z,k}^{retro}$ |
| 构型折算寿命 | $n_k$ |
| 冷板带走 IT 热量比例 | $\eta_i^{CP}$ |

### 热安全参数

| 缺失数据 | 对应模型项 |
|---|---|
| 有效空气侧冷却量对进风温度的抑制系数 | $\beta_i$ |
| 机柜最高允许进风温度 | $T_i^{max}$ |
| 老旧空调基准送风温度 | $T_{sup,z}^{old,0}$ |
| 老化导致的送风温度抬升 | $\Delta T_z^{age}$ |
| 新空调送风温度设定值 | $T_{sup}^{new}$ |

### 运行效率与供热

| 缺失数据 | 对应模型项 |
|---|---|
| 空气源热泵 COP | $COP^{ASHP}$ |
| 水源热泵 COP | $COP^{WSHP}$ |
| CDU/冷板循环泵单位耗电 | $e^{CDU,pump}$ |
| WSHP 取热回路循环泵单位耗电 | $e^{WSHP,pump}$ |
| CDU 至冷却塔自然冷却支路附属耗电 | $e^{CDU,free}$ |
| 冷却塔单位排热风机耗电 | $e^{tower}$ |
| 外部供热需求曲线 | $Q_t^{heat,demand}$ |
| 供热收益价格 | $Price^{heat}$ |
| 被替代供热碳因子 | $cf^{boiler}$ |

### 经济性、隐含碳与承重

| 缺失数据 | 对应模型项 |
|---|---|
| 各容量设备单位容量投资 | $c^m$ |
| 各容量设备单位容量隐含碳 | $EI^m$ |
| 各容量设备折算寿命或资本回收系数 | $n^m$、$CRF^m$ |
| 固定运维和可变运维参数 | $C^{ann}_{FOM}$、$C_t^{VOM}$ |
| 机柜改造前重量 | $W_i^{old}$ |
| 冷板/RDHX 改造增加的机柜重量 | $w_i^{CP}$、$w_i^{RD}$ |
| 机柜最大允许重量 | $W_i^{rack,max}$ |
| 各构型新增固定设备重量 | $W_{z,k}^{equip}$ |
| 新空调、RDHX、CDU 单位容量重量 | $w^{AC,new}$、$w^{RDHX}$、$w^{CDU}$ |
| 区域楼板承载上限和承载核算面积 | $Load_z^{floor,max}$、$Area_z$ |

## 最优先补充顺序

1. 既有空调、Chiller、冷却塔容量，以及各改造设备容量上限。
2. 各设备单位投资、隐含碳、寿命和固定改造成本。
3. 冷板热量分流比例；RDHX 按 100% 吸收 IT 热量处理。
4. 楼板承重、机柜重量和新增设备重量。
5. 供热需求、供热价格和替代供热碳因子。
6. 热安全校准参数，尤其是 $\beta_i$、$T_i^{max}$ 和送风温度参数。
