# 模型参数取值

本文档用于集中记录论文建模中已经由数据拟合或外部数据确定的参数取值。若后续参数按区域 $z$ 或时段 $t$ 区分，可在本表基础上扩展为分区表或时间序列表。

## 已拟合参数

| 参数符号 | 当前取值 | 单位 | 适用口径 | 来源或备注 |
|---|---:|---|---|---|
| $e_z^{RDHX}$ | 0.0265 | kWe/kWth | RDHX 背板整体单位冷量耗电系数 | 由数据拟合得到，包含 RDHX 末端侧与循环泵等附属耗电，不再拆分 terminal 与 pump |
| $COP_t^{Chiller}$ | 3 | - | 冷机 COP | 当前按常数处理，若后续有环境工况或冷冻水温度数据，可扩展为时变参数 |
| $e_z^{old,fan}$ | 0.2137 | kWe/kWth | 既有空调末端风机单位冷量耗电系数 | 由数据拟合得到 |
| $e_z^{new,fan}$ | 0.2137 | kWe/kWth | 新空调末端风机单位冷量耗电系数 | 当前暂用同一空调末端风机拟合值；若后续获得新空调样本数据，可单独更新 |

## 使用说明

- 若模型暂不区分区域 $z$，上述带 $z$ 的参数可对所有区域取同一常数。
- 若后续能够区分不同空调机组区域，可将 $e_z^{RDHX}$、$e_z^{old,fan}$ 和 $e_z^{new,fan}$ 扩展为按 $z$ 取值。
- $COP_t^{Chiller}$ 当前取常数 3，等价于对所有时段 $t$ 采用同一冷机 COP。
